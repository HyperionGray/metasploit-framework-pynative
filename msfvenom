#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
Metasploit Framework Msfvenom - Python Version

Wrapper script for msfvenom.

Converted from Ruby msfvenom script.
"""

import sys
import os
from pathlib import Path


def main():
    """Main entry point for msfvenom."""
    
    # Get the repo root
    repo_root = Path(__file__).parent.resolve()
    
    # Delegate to the Ruby version (now msfvenom.rb)
    ruby_script = repo_root / "msfvenom.rb"
    if ruby_script.exists():
        try:
            # Execute the Ruby version with all arguments
            os.execv(str(ruby_script), ['msfvenom'] + sys.argv[1:])
        except Exception as e:
            print(f"Error executing Ruby msfvenom: {e}", file=sys.stderr)
            sys.exit(1)
    else:
        print("Error: Ruby msfvenom not found", file=sys.stderr)
        print("Native Python version implementation pending Ruby removal", file=sys.stderr)
        sys.exit(1)


if __name__ == "__main__":
    try:
        main()
    except KeyboardInterrupt:
        print("\nAborting...")
        sys.exit(1)


